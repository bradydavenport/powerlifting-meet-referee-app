import HeadRef from './HeadRef/HeadRef';
import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View, Button, Alert, Pressable } from 'react-native';

export default function App() {
  return (
    <HeadRef />
  );
}

